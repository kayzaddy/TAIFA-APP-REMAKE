# 5. Merchant Acceptance Guide

Tap sessions bind to `enterprise.Merchant` + optional `AcceptanceTerminal`.

Channels: `nfc` · `softpos` · `pos`

Retail, SoftPOS, transit, gov, hospital, education — same intent + capture path.
