# 6. Security Manual

- Device-bound API auth (`IsDevice`)
- Intent HMAC (MAP)
- Idempotency-Key on confirm
- Auth required gate before capture
- No MAP-side balances
- Minimize merchant PII (display name only on receipt)
- Hardware keystore / attestation — future
