# API contracts

**Owner:** TIP + Architecture  
**Purpose:** OpenAPI, GraphQL schemas, event schemas, contract tests.

| Path | Content |
| --- | --- |
| `openapi/` | REST contracts (source of truth) |
| `events/` | CloudEvents payload schemas |
| `contracts/` | Pact / AsyncAPI |

Register all public APIs in TIP before external exposure.
