# === TAIFA Backend — Payment Service ===
# Python 3.11 (Django 5.x does not yet support 3.14).

Django==5.2.17
djangorestframework==3.15.2
django-cors-headers==4.6.0

# API contract (OpenAPI 3 schema + Swagger UI / Redoc)
drf-spectacular==0.28.0

# Error tracking / performance monitoring (activated only when SENTRY_DSN is set)
sentry-sdk==2.20.0

# Database
psycopg[binary]==3.2.4
dj-database-url==2.3.0

# Config
python-dotenv==1.2.2
python-dateutil==2.9.0.post0  # month-safe recurring-payment scheduling (relativedelta)

# HTTP (M-Pesa Daraja + other providers)
requests==2.33.0

# Async processing (webhooks, retries, reconciliation)
celery==5.6.3
redis==5.2.1

# Real-time trip/driver tracking
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.2
cryptography==50.0.0

prometheus-client==0.21.1

# OpenTelemetry (optional at runtime — activated when OTEL_EXPORTER_OTLP_ENDPOINT is set)
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp-proto-http==1.29.0
opentelemetry-instrumentation-django==0.50b0
opentelemetry-instrumentation-requests==0.50b0
opentelemetry-instrumentation-celery==0.50b0

# Server (production) — gunicorn managing uvicorn ASGI workers so HTTP and
# WebSockets (Channels: real-time trip/driver tracking) share one process model.
gunicorn==23.0.0
uvicorn[standard]==0.34.0

# JWT (taifa_merchant BFF auth)
PyJWT==2.10.1
