# Copy to backend.tf after state bucket exists (shared account).
# Do not commit backend.tf with account-specific secrets.

# terraform {
#   backend "s3" {
#     bucket         = "taifa-terraform-state-ACCOUNT_ID"
#     key            = "env/staging/terraform.tfstate"
#     region         = "af-south-1"
#     dynamodb_table = "terraform-locks"
#     encrypt        = true
#   }
# }
